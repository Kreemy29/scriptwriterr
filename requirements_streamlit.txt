# Streamlit Cloud Production Requirements
# Optimized for fast deployment and minimal memory usage

# Core Streamlit
streamlit>=1.37.1

# Database & ORM
sqlmodel>=0.0.16
sqlalchemy>=2.0.0
pydantic>=1.10.15

# API & Environment
requests>=2.32.3
python-dotenv>=1.0.1

# Data Processing (minimal versions for cloud deployment)
pandas>=2.0.0
numpy>=1.24.0

# AI/ML Libraries (optimized for Streamlit Cloud)
scikit-learn>=1.3.0
sentence-transformers>=2.2.0

# PyTorch CPU-only for Streamlit Cloud (much smaller)
torch>=2.0.0
transformers>=4.30.0

# Optional: Uncomment if using advanced features
# plotly>=5.0.0
# altair>=5.0.0
